version: 2

models:
    - name: dim_locations
      description: store locations further classified by country
      columns:
        - name: id
          tests:
            - not_null

models:
    - name: dim_customers
      description: store customer info
      columns:
        - name: id
          tests:
            - not_null