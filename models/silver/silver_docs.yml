version: 2

models:
    - name: dim_locations
      description: store locations further classified by country
      columns:
        - name: id
          tests:
            - not_null
    - name: dim_customer
      description: store customer details
      columns:
        - name: unique_id
          tests:
            - not_null
